import styled from "styled-components";

type StyleProps = {
  fontColor: string
};

export const Styled{{ pascalCase name }} = styled.p<StyleProps>(({ theme, fontColor }) => ({
  fontSize: theme.typography.headlineMedium,
  fontColor: fontColor,
}));