import styled from "styled-components";

type StyleProps = {
  fontColor: string
};

export const Styled{{ pascalCase name }} = styled.h4<StyleProps>(({ theme, fontColor }) => ({
  background: theme.palette.default.light,
  fontColor: fontColor,
}));